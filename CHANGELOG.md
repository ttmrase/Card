# 更新履歴

不具合の切り分けをしやすくするための記録。
各項目に「何が起きていたか」「原因」「どこを直したか」を残しています。

書式は日付と通し番号。新しいものが上。

---

## 2026-08-15 (17) — 「または」、〜したターン、対象になった、フェイズ操作

### 追加

**条件の「または」**

- 条件に「**いずれかを満たす（または）**」を追加。
  この中に並べた条件は、どれか1つを満たせば発動できる。
- これまでどおり、条件を並べて追加すると「かつ」でつながる。
- 「または」の中にも条件を入れ子にできる。

  ```
  ①：【条件】自分のライフが2000以下である、または自分の墓地のカードが3枚以上存在する ⇒ …
  ```

**「〜したターン」**

- 出来事の条件に「**いつまで見るか**」を追加。
  - 「〜した場合」…これまでどおり、その瞬間に発動する誘発効果になる
  - 「〜したターン」…**そのターンにそれが起きていれば満たす**発動条件になる
- 「〜したターン」は誘発効果ではないので、自分のタイミングで発動できる。

  ```
  ①：【条件】自分のモンスターが召喚・特殊召喚されたターン ⇒ …
  ```

**「対象になった」**

- 出来事に「**効果の対象になった**」を追加。
  効果が「選んで」対象を取ったときに、選ばれたカードへ知らされる。
- あわせて、出来事の条件に「**出来事を起こしたカードを限定する**」欄を追加。
  これで次のように書ける。

  ```
  ①：【条件】このカードが罠カードによって効果の対象になった場合 ⇒ …
  ```

- この欄は他の出来事にも使えるので、「相手の魔法カードによって破壊された場合」なども書ける。

**「このカードの効果によって特殊召喚された」**

- 対象の条件に「**このカードの効果で特殊召喚された**」を追加。
- 効果を与える効果と組み合わせると、次のように書ける。

  ```
  ②：このカードがフィールドに存在する限り、自分モンスターゾーンの全ての
  このカードの効果によって特殊召喚されたモンスターは次の効果を得る「…」
  ```

**フェイズ・ターンを進める効果**

- 述語に「**フェイズ・ターンを進める**」を追加。3つから選ぶ。
  - そのフェイズをスキップする
  - エンドフェイズにする
  - ターンを終了する
- 効果の処理が**全て終わってから**適用される（処理の途中で盤面が進むと壊れるため）。
- 相手ターンに使いたい場合は【誘発即時】を付ける。

### 修正

**発動の処理中に、同じカードをもう一度発動できてしまった**

- 現象：罠を発動 → その処理で相手の誘発効果が発動 → その発動に対して**同じ罠がもう一度**
  発動し、効果が2回処理されていた。
- 原因：発動した魔法・罠は解決の途中は表側でフィールドにあるため、
  「まだ発動していないカード」と同じに見えていた。
- 直した場所：`GameEngine`。発動を処理している最中のカードは、
  その解決が終わるまで発動候補に出さないようにした。
- 「対象になった」の追加で見つかった、以前からあった不具合。

---

## 2026-08-15 (16) — 効果の付与、処理のまとめ、枚数を参照するドロー

### 追加

**効果を与える効果**

- 述語に「**効果を与える（永続向き）**」を追加。
  【発動タイプ】を「永続」にした効果に書くと、このカードが【場所】にある間だけ、
  指定したカードがその効果を持つようになる。

  ```
  ①：このカードがフィールドに存在する限り、自分モンスターゾーンの全てのモンスターは
  次の効果を得る「自分のライフを700ポイント回復する。」
  ```

- 与えられた効果は、そのカード自身の効果として扱われる。
  発動する効果も永続の効果も与えられ、与え手が場を離れれば効果も消える。
- 与えられた効果は自前の効果の後ろに並ぶので、①②…の番号はずれない。
- 「効果を与える効果」を与えることはできない（無限に増えるため）。

**処理をまとめて1つの手順にできるようにした**

- 効果の一覧で、各行の「**別／一体**」をタップすると前の処理と1つにまとまる。
- まとまった処理は「〜し、〜する」とつながり、まとまりの先頭を「任意」にすると
  **まとめて行うかどうかを一度だけ**選ぶ形になる。
- これで次のような効果が書けるようになった。
  以前は別々の処理だったため、見せずに墓地送りだけしたり、見せるだけで終わったりしていた。

  > その後、自分は手札を相手に見せ、手札の「VALIS」魔法カードの数だけ
  > デッキから「VALIS」モンスターを選んで墓地に送ることが出来る。

- 「カードを見せる」処理は対象が無くても失敗しないので、
  手札が空でもまとまりの途中で止まらない。

**枚数を参照するドロー・墓地送り・手札を捨てさせる**

- ドロー／デッキから墓地へ送る／手札を捨てさせるの枚数を、
  ほかの効果と同じ「枚数を別のカードの枚数から決める」で指定できるようにした。
- 前回の更新でドローの枚数指定はデータの上では作ってあったが、
  **編集画面から設定できていなかった**（枚数の入力欄が固定値のままだった）ので、
  3つとも枚数指定のエディタに差し替えた。

### 変更

- まとめた処理の文をつなぐとき、前の文を連用形にするようにした
  （「見せるし、」→「見せ、」、「破壊するし、」→「破壊し、」）。
- サンプルの「アララギの号令」を、見せる処理と墓地送りをまとめた形にした。

---

## 2026-08-15 (15) — 誘発即時効果、処理ごとの任意／強制、召喚制限を【制限】へ

### 追加

**誘発即時効果**

- 効果ごとに「**誘発即時**」のチェックを追加。付けると次のようになる。
  - 相手のターンでも発動できる
  - 自分のターンでも、メインフェイズ以外（バトルフェイズなど）で発動できる
  - 相手の発動・召喚・攻撃宣言への割り込みの候補に出る
- カードテキストには【誘発即時】と書かれる。
- 罠カードと、【場所】がフィールド以外の**モンスター効果**（手札誘発の形）は、
  これまでどおりチェック無しでも割り込める。

**処理ごとの任意／強制**

- ひとつの効果の中の処理を1つずつ「任意」「強制」で切り替えられるようにした。
  効果の一覧で、各行の「強制／任意」をタップすると切り替わる。
- 任意にした処理は「〜することができる」と書かれ、効果の処理時に確認が出る。
- **任意の処理は発動できるかどうかの判定に数えない**ので、
  「サーチは必ず、その後の墓地送りは任意」のような効果が作れる。
  強制の処理はこれまでどおり、最後まで処理できないと発動できない。

### 変更

**召喚制限を効果ではなく【制限】に移した**

- 効果（述語）としての「召喚・特殊召喚を制限する」を廃止し、
  【制限】の欄に「**召喚の縛り**」として持たせた。カード全体にも効果ごとにも書ける。
- 発動そのものに付く制限になったので、**効果を無効にされても掛かったまま**になる。
  以前は効果として処理していたため、無効にされると制限も消えていた。
- カードテキストでは【制限】の行に並ぶ。

  ```
  【制限】このカードを発動するターン、自分は「VALIS」モンスター以外のモンスターを特殊召喚できない
  ①：自分のデッキの「VALIS」魔法カード1枚を選んで手札に加える。その後、…
  ```

- 効果として作ってあった旧データは、読み込み時に自動で【制限】へ移す。

**手札誘発の暗黙ルールをモンスターだけに絞った**

- 前回は「【場所】がフィールド以外の効果は割り込める」としていたが、
  これだと【場所】に手札と書いた**魔法カード**まで相手ターンに撃ててしまう。
  暗黙で割り込めるのはモンスター効果だけにし、魔法は「誘発即時」を付けたときだけにした。

- 誘発即時効果は、相手ターンでも普通のカード操作から発動できる
  （割り込みの確認を待たなくてよい）。

---

## 2026-08-15 (14) — 枚数の参照・召喚制限・カードの公開

次のカードを再現できるようにするための3つの追加。

> このカードを発動するターン、自分は「VALIS」モンスター以外のモンスターを特殊召喚できない。
> ①：デッキから「VALIS」魔法カード1枚を選び、手札に加える。その後、自分は手札を相手に見せ、
> 手札の「VALIS」魔法カードの数だけデッキから「VALIS」モンスターを選んで墓地に送ることが出来る。

### 追加

**枚数を別のカードの枚数から決められるようにした**

- 対象指定に「枚数を別のカードの枚数から決める」を追加。
  「手札の『VALIS』魔法カードの数だけ、デッキから『VALIS』モンスターを墓地へ送る」が書ける。
- 墓地へ送る・除外する・手札に加える・デッキに戻す・破壊する・特殊召喚するなど、
  対象指定を使う効果すべてで使える。**ドローの枚数**にも使える。
- あわせて「**〜まで**」を追加。付けると対象が足りなくても発動でき、あるだけ処理する。
  付けない場合はこれまでどおり、指定の数がそろわないと発動できない。

**召喚制限を掛ける効果を追加した**

- 述語に「召喚・特殊召喚を制限する」を追加。次の組み合わせで書ける。
  - 制限を受ける側（自分／相手／両方）
  - 制限する召喚（特殊召喚／通常召喚／両方）
  - 対象のカードの条件（カテゴリ・属性・種族・レベルなど）
  - 「指定したカード**以外**を出せなくする」の切り替え
- 制限はそのターンの間だけ続き、ターンが変わると消える。
- 【発動タイプ】を「発動時」にすると、**効果を使うかどうかに関わらず、発動しただけで掛かる**。
  この形の効果はカードテキストでも効果番号を付けず、①より前の行に書かれる。

**カードを相手に見せる（公開する）効果とコストを追加した**

- 述語とコストの両方に「カードを相手に見せる（公開する）」を追加。見せ方は3種類。

  | 見せ方 | 意味 |
  | --- | --- |
  | その場だけ | 見せて終わり。ログに残る |
  | このターンの間 | そのターンのあいだ、相手の画面にも表で見える |
  | ずっと | その領域にある限り、相手の画面にも表で見える |

- 公開されたカードは、相手の手札の欄に表向きで並ぶ。領域を移ると公開は解ける。

### 修正

**魔法・罠の「発動時」の効果が、番号の効果を発動したときに処理されなかった**

- 現象：「発動時」の効果と番号付きの効果を両方持つ魔法を発動すると、
  どちらか一方しか処理されなかった。
- 原因：「カードの発動」と「①の発動」を別々の操作として扱っていたため。
  魔法・罠は番号の効果を発動することがそのまま「カードの発動」なので、
  「発動時」の効果も一緒に処理されるべきだった。
- 直した場所：`GameEngine.activateClause`。手札から出す／伏せから表にする場合は、
  番号の効果より先に「発動時」の効果を処理する。
  発動する番号を選ぶ画面にも「このカードを発動する」を二重に並べないようにした。

**書き出し／取り込みで貼り替えられない参照があった**

- 魔法・罠ゾーンに置く・発動する・耐性を与える・攻撃を封じる各効果と、
  カードを動かすコスト、数値の「枚数×係数」、場合分け（●）の中身が対象外だった。
  別の端末に持っていくとカテゴリや属性の指定が外れる可能性があったので、全て貼り替えるようにした。

### 変更

- サンプルに「アララギの号令」を追加。上の3つをまとめて使っている。

---

## 2026-08-15 (13) — 手札のモンスターの効果、テキストの読みやすさ、AIターン

### 修正

**手札のモンスターの効果が発動できなかった**

- 現象：【場所】に手札を指定したモンスター効果（手札誘発や、自身を特殊召喚する効果）が、
  条件を満たしていても発動できなかった。
- 原因：エンジンは発動できる状態だったが、**デュエル画面のカード操作ダイアログに
  「効果を発動する」ボタンが無かった**。手札のモンスターには
  「通常召喚する」「裏側守備でセットする」しか出していなかった。
- 直した場所：`DuelScreen.CardActionDialog`。手札のモンスターにも
  「効果を発動する」を出すようにした。

**発動できない理由の表示が間違っていた**

- 現象：「このカードが手札に存在する場合」のような自分自身を見る【条件】を持つカードが、
  実際は【コスト】が払えないだけなのに「【条件】を満たしていない。」と表示されていた。
- 原因：`whyCannotActivate` が条件の判定にカード自身を渡していなかったため、
  「このカードが〜」という条件が常に不成立になっていた。
- 直した場所：`GameEngine.whyCannotActivate`。

### 変更

**カードテキストの区切りを分かりやすくした**

- 【…】の欄どうしは「／」、前置きと効果そのものの間は「⇒」で区切るようにした。
  改行が増えず、「【効果】」と書き足す必要も無い。

  > ①：【条件】このカードが手札に存在する場合 ／ 【コスト】自分の墓地の「VALIS」モンスター1体をデッキの一番下に戻す ⇒ このカードを自分のモンスターゾーンに表側攻撃表示または表側守備表示で特殊召喚する。

- 画面表示ではさらに色を分けた。【…】の見出しは紫、①②の番号は金色、
  前置きは淡い色、効果そのものは通常の色。

**効果による攻守の増減を盤面に出した**

- モンスターゾーンの攻守を、永続効果・一時的な増減を含めた**現在の値**にした。
- 素の値から上がっていれば緑、下がっていれば赤、変化なしなら従来どおり金色。
  変化しているときは太字にもしている。

**AIのターンが速すぎて追えなかったのを直した**

- AIの1手ごとに約0.7秒の間を置くようにした（テストでは0秒のまま）。
- **相手の行動に割り込める範囲を広げた。**
  これまで割り込めたのはセットしてある罠だけだったが、
  **【場所】にフィールド以外（手札・墓地・除外ゾーン）を書いた効果**も
  相手ターンに発動できるようにした。いわゆる手札誘発がこれにあたる。
  フィールドのカードの起動効果は、これまでどおり自分のターンのみ。
- 割り込みの機会は、相手のカードの発動時・召喚時・攻撃宣言時に開く。

---

## 2026-08-15 (12) — 「相手の効果によって」を書けるようにした

### 追加

**【条件】のイベント条件に「何が原因で起きたか」を指定できる**

これまでイベント条件（「〜した場合」）は、出来事が起きた事実だけを見ていた。
「相手の効果によってフィールドを離れた場合」のように、原因を限定できなかった。

条件の編集画面に「何が原因で起きたか」を追加。次から選ぶ。

| 選択肢 | 意味 |
| --- | --- |
| 原因を問わない | これまでどおり。何が原因でも誘発する |
| 効果によって | どちらかのカードの効果が原因のとき |
| 相手の効果によって | 相手が発動した効果が原因のとき |
| 自分の効果によって | 自分が発動した効果が原因のとき |
| 戦闘によって | 戦闘破壊が原因のとき |

- 「相手・自分」は、**その条件を持つカードの持ち主**から見た呼び方。
- 「このカード自身が対象のときだけ」と併用できる。
  → 「このカードが相手の効果によってフィールドを離れた場合」
- カードテキストにもそのまま出る。

### 仕組み

- 盤面の出来事（`GameEvent`）に「原因」と「原因を作ったプレイヤー」を持たせた。
- 効果の処理中とコストの支払い中は「効果が原因」として記録する
  （コストも、そのカードを発動した側の効果として扱う）。
- 戦闘破壊は「戦闘が原因」として記録する。
- 通常召喚のリリースやルール上の処理は「原因を問わない」のまま。

### 変更

- サンプルの「残響の巨神」を「相手の効果によってフィールドを離れた場合」にした。
  これで、次のテキストがそのまま作れる。

  > このカードが相手の効果によってフィールドを離れた場合に発動できる。
  > 送られた場所によって以下の効果を適用する。
  > ●墓地：デッキからレベル10以上のモンスター1体を選び、手札に加える。
  > ●除外ゾーン：このカードを手札に加える。

- 既に作ったカードは「原因を問わない」として読み込まれるので、動きは変わらない。

---

## 2026-08-15 (11) — 特殊召喚の表示形式を選べる、制限の適用範囲

### 追加

**特殊召喚する表示形式をプレイヤーが選べる**

- カード作成時に、選べる表示形式を複数指定できる。
  2つ以上選ぶと、効果を処理するときにプレイヤーが選ぶ。1つだけなら固定。
- カードテキストは「表側攻撃表示または表側守備表示で特殊召喚する」となる。
- サンプルの「アララギの儀式」を、攻撃／守備を選べる形にした。

**効果番号より前に書く【制限】に、適用範囲を指定できる**

効果ごとに制限を書くとテキストが長くなるため、共通の欄で次のように書き分けられる。

| 書き方 | 意味 |
| --- | --- |
| 効果を選ばない＋まとめて | このカードは1ターンに1度（これまでどおり） |
| 効果を選ばない＋効果ごと | **それぞれの効果**が1ターンに1度ずつ |
| ②③を選ぶ＋まとめて | **②③のうちいずれか1つ**が1ターンに1度 |
| ①②を選ぶ＋効果ごと | ①②が**それぞれ**1ターンに1度ずつ |

- 効果ごとに書く【制限】は今までどおりで、そのまま使える。
- サンプルの「知識の巻物」を「それぞれの効果は同名カードを含めて1ターンに1度」にした。

---

## 2026-08-15 (10) — 追加済みの項目を編集、場合分け効果

### 修正

**効果でカードを動かしたあと、【発動後】がそれを上書きしていた**

- 効果自身が「このカードを手札に加える」などでカードを動かした場合でも、
  そのあと【発動後】の処理が走ってカードが別の場所へ送られていた。
- 効果がカードを動かしていたら、そのままにするよう直した。

### 追加

**追加済みの【条件】【コスト】【制限】を編集できる**

- これまでは削除しかできず、直すには消して作り直す必要があった。
  一覧の項目をタップすると、その内容が入った状態でダイアログが開く。
- 効果の文（述語）は以前からタップで編集できる。

**場合分け効果（●）**

- ひとつの効果の中に「●条件：効果」を並べられる。
  「送られた場所によって以下の効果を適用する」のような書き方ができる。
- 当てはまるものの扱いは3つから選ぶ。
  最初に当てはまるものだけ／当てはまるものを全て／当てはまるものから1つ選ぶ。
- 場合分けのうち、当てはまって最後まで処理できるものが1つも無ければ発動できない。

これに合わせて次を追加した。

- 【条件】の「このカードが〇〇にある」…この効果を持つカード自身の居場所を見る。
- 出来事の「フィールドを離れた」…破壊・除外・手札に戻る・墓地送りをまとめて拾う。
- 【場所】に「除外ゾーン」…除外されたカードから発動できるようになる。

サンプルに「残響の巨神」を追加。カードテキストは次のように表示される。

```
【場所】墓地、除外ゾーン
①：【条件】このカードがフィールドを離れた場合 以下のうち、最初に当てはまるものを適用する。
●このカードが墓地に存在する場合：自分のデッキのレベル10以上のモンスター1体を選んで手札に加える。
●このカードが除外ゾーンに存在する場合：このカードを手札に加える。
```

---

## 2026-08-15 (9) — 柔軟なコスト、処理できる効果だけ発動、選んで受け渡し、マスター編集

### 変更

**【コスト】を効果と同じ自由度にした**

- コストに「カードを動かす」を追加。効果と同じ対象指定（誰の・どの領域の・
  どんなカードを・何枚）で選び、墓地／除外／手札／デッキの上／デッキの下へ動かす。
- これで「墓地のカード2枚をデッキの一番下に戻して発動する」のような書き方ができる。
  これまでの手札を捨てる・リリース・墓地から除外も、この1つで書ける
  （古い書き方のカードもそのまま動く）。
- サンプルに「巡りの祈り」を追加。

**効果を最後まで処理できないと発動できないようにした**

- 「〜し、その後〜する」のように処理が複数ある効果は、**書いてある順に
  最後まで処理できる**場合だけ発動できる。
- 前の処理で動いたカードは差し引いて数える。
  例：「相手モンスター1体を破壊し、その後1体を除外する」は相手に2体必要。
- 発動できないときは「効果を最後まで処理できる対象がそろっていない。」と表示する。

### 追加

**属性・種族・カテゴリを、カード作成中に編集・削除できる**

- 各欄の横のボタンから、一覧・追加・名前変更・削除ができる管理画面を開ける。
  これまでは専用画面（ホームの「属性・種族・カテゴリ」）でしか直せなかった。
- 名前を変えると、それを使っている全てのカードの表示も変わる。
  削除すると、使っていたカードからは外れる。

**カード・デッキを選んで書き出し／取り込み**

- 書き出しは、カードとデッキをチェックして選ぶ。デッキを選ぶと、その
  デッキが使っているカードも自動で選ばれる。
- 取り込みも、ファイルの中身を一覧で見てから選べる。長押しで効果も確認できる。

---

## 2026-08-15 (8) — 枚数で決まる数値、フェイズ条件、切り抜き修正、テキストの手直し

### 修正

**取り込んだ画像の透明部分が黒く潰れていた**

- 内部保存を常に JPEG にしていたため、透過のある画像（PNG など）の
  透明部分が黒になっていた。「切り抜きが効いていない」ように見えたのはこれ。
- 透過がある画像は PNG で保存してアルファを残すようにした。

**切り抜き画面で画像の全体が見えなかった**

- 画像を枠いっぱいに表示していたため、どこが切り取られるのか分かりにくかった。
- 画像全体を表示したうえで、中央のカード枠（3:4）以外を暗く覆う形に作り直した。
  枠の外も残っているので、ドラッグとつまむ操作で自由に位置を決められる。

### 追加

**数値を「カードの枚数×係数」で決められる**

- 攻撃力・守備力の変化、ダメージ、ライフ回復の数値を、固定値だけでなく
  「条件を満たすカードの枚数 × 係数（＋固定値）」で指定できる。
- 数える対象は、いつもの対象指定（誰の・どの領域の・どんなカード）で決める。
- サンプルに「累なる怨嗟」（自分の墓地のモンスターの数×200のダメージ）を追加。

```
①：相手に自分の墓地のモンスターの数×200ポイントのダメージを与える。
```

**【条件】に「〇〇フェイズである」**

- ドロー／メイン1／バトル／メイン2／エンドから選べる。
- 指定した効果は、そのフェイズでだけ発動できるようになる。
  指定が無い場合はこれまでどおり、魔法とモンスターの効果はメインフェイズのみ。
- **フェイズが変わるときに発動確認が出る。** エンドフェイズのように
  操作の手が無いフェイズでも、そのフェイズを指定した効果を使える。
- サンプルに「宵の帰還」（エンドフェイズに墓地のモンスターを手札に戻す罠）を追加。

**効果テキストの手直し**

- 自動で組み立てた文の助詞などが不自然なとき、表示だけを書き換えられる。
  ゲーム中の動作は効果データのままで変わらない。
- カード編集画面の「効果テキストの手直し」から、いまの文を読み込んで編集する。
  「自動生成し直す」で作り直し、「手直しをやめる」で自動生成に戻せる。

---

## 2026-08-15 (7) — 発動時処理、置く／伏せる／発動する、属性・種族の追加、切り抜き

### 修正

**永続の効果しか持たない魔法・罠が場に出せなかった**

前回の版で永続効果を「発動タイプ」に統合したが、発動できる効果が無いカードは
発動の候補から外れてしまい、永続魔法をフィールドに出す手段が無かった。
下記の「カードの発動」で解決した。

**永続の効果が、手札にあるうちから効いていた**

適用場所の判定に「そこから発動できるか」を使っていたため、
【場所】がフィールドの永続効果が、手札にある間から働いていた。
実際にその場所に置かれていることを条件にするよう直した。

### 追加

**発動時の効果処理**

- 【発動タイプ】に **「発動時（このカードの発動時のみ処理する）」** を追加。
  そのカード自体を発動したときにだけ処理される効果になる。
  場に残ったあとで単独では発動できない。
- あわせて「カードの発動」という操作を追加した。発動時処理か永続の効果を持つ
  魔法・罠は、効果を選ばずカードそのものを発動してフィールドに出せる。
  発動できる効果も持っている場合は、どちらを行うか選ぶ。
- 表側で場に出ているカードは発動済みとして、もう一度は発動できない。

```
【発動後】そのまま残す
①：このカードの発動時に、自分はカードを1枚ドローする。
②：このカードがフィールドに存在する限り、
　　自分モンスターゾーンの全ての「アララギ」モンスターの攻撃力は500アップする。
```

**魔法・罠ゾーンに置く／伏せる／発動する述語**

- 「魔法・罠ゾーンにセットする」…裏側で置く。伏せたターン扱いになる。
- 「魔法・罠ゾーンに表側で置く」…発動しないので、永続の効果だけが働く。
- 「そのカードを発動する」…デッキや手札のカードをその場で発動する。
- サンプルに「先読みの書」（デッキから罠を1枚セットする）を追加。

**属性・種族もカード作成画面から追加できるように**

- カテゴリと同じく、属性と種族の欄の横の「＋」から新規作成できる。
  作った項目はそのままそのカードに設定される。

**イラストの切り抜き**

- 画像を選んだあと、切り抜き画面が開く。指でドラッグして位置を、
  つまんで拡大縮小を決め、カードのイラスト枠（3:4）に収める。
- あとから「切り抜き」ボタンでやり直せる。

---

## 2026-08-15 (6) — 永続効果を、専用欄から既存の効果の枠組みへ

### 変更

**【永続効果】欄を廃止し、効果の【発動タイプ】に「永続」を追加**

前回は永続効果を専用の欄として足したが、それだと書ける内容が
その欄で用意した3種類に固定されてしまい、他の効果と組み合わせられなかった。
【場所】【条件】【効果】といった既存の部品をそのまま使えるようにした。

- 【発動タイプ】に **「永続（発動せず、その場所にある限り適用）」** を追加。
  これを選ぶと、そのカードが【場所】にある限りずっと適用される効果になる。
- カードテキストは「このカードが〜に存在する限り、〜」という書き出しになる。

```
①：このカードがフィールドに存在する限り、
　　自分モンスターゾーンの全ての「アララギ」モンスターの攻撃力は500アップする。
```

この書き方により、次のことが既存の部品だけでできる。

- 【場所】を墓地にすれば「墓地に存在する限り」の永続効果になる。
- 【条件】を付ければ「自分のライフが2000以下の間だけ」のように、
  状況によって効いたり切れたりする永続効果になる。
- 1枚のカードに、永続の効果と発動する効果を①②として並べられる。

### 追加

- 対象指定に **「この効果を持つカード自身」** を追加。
  永続効果はもちろん、「このカードを破壊する」「このカードの攻撃力を上げる」
  といった発動する効果でも使える。
- 述語に **「耐性を与える」「攻撃できなくする」** を追加。
  【発動タイプ】が「永続」なら常に適用され、発動する効果に書くと
  **そのターンの間だけ**適用される。
  （例：このターン、自分のモンスターは戦闘では破壊されない）
- サンプルに永続魔法「アララギの旗印」を追加。
  「不滅の守護者」も新しい書き方に置き換えた。

### 互換性

保存済みのカードが持っていた旧【永続効果】欄は、読み込み時に
「永続」の効果へ自動で変換される。変換後は他の効果と同じように編集できる。

---

## 2026-08-15 (5) — エクスポート／インポート、長押しで効果確認、永続効果

### 追加

**カード・デッキの書き出しと取り込み**

- カード一覧の右上から、全カードとデッキを JSON ファイルとして書き出せる。
  デッキ一覧では、デッキと、そのデッキが使っているカードだけを書き出せる。
- 同じ画面から取り込みもできる。取り込みの決まりは次のとおり。
  - 属性・種族・カテゴリは、**同じ名前**のものが既にあればそれを使い回す。
    無ければ追加する。カードの中の参照（効果の絞り込み条件、カテゴリ制限など）も
    まとめて貼り替えるので、別の端末で作ったカードもそのまま動く。
  - カードは ID が一致するものを上書きする。同じファイルを2回取り込んでも
    増えていかない。
- イラストはファイルに Base64 で埋め込むので、1ファイルで受け渡せる。
- 併せて、取り込んだイラストは長辺1024pxの JPEG に縮小して保存するようにした。
  端末の写真をそのまま抱えなくなるので、保存容量も書き出しファイルも軽くなる。

**長押しで効果を確認**

- カード一覧、デッキ編集、デュエル中の手札・フィールド、
  そして**デッキをサーチしているときの選択画面**で、長押しするとカードの
  イラストと効果テキストを確認できる。
- デュエル中は、永続効果を反映した実際の攻守が表示される。

**【永続効果】**

- 発動を必要とせず、表側でフィールドにある限りずっと適用される効果。
  カード編集画面の「永続効果」から追加する。
  - 耐性を与える：相手の効果を受けない／戦闘では破壊されない／効果では破壊されない
  - 攻撃力・守備力を増減する
  - 攻撃できなくする
- 適用先は「このカード自身」か、対象指定（自分フィールドの全ての岩石族モンスター、など）。
- 「相手の効果を受けない」カードは、相手の効果の対象そのものに選べなくなる。
  自分の効果では今までどおり選べる。
- 攻守の増減は、戦闘計算・効果の絞り込み・画面表示の全てに反映される。
- サンプルに「不滅の守護者」を追加。

---

## 2026-08-15 (4) — 効果テキストの書式統一、イベント条件、任意／強制の区別

### 変更

**【発動後】が既定どおりのときは書かない**

- 通常魔法のように「発動後は墓地へ送る」カードで、毎回【発動後】墓地へ送る と
  表示されていたのをやめた。既定と違う指定をしたときだけ書く。
- 既定は、魔法・罠が「墓地へ送る」、モンスターが「そのまま残す」。

**モンスターの効果テキストを、他のカードと同じ書式に統一**

- これまでモンスターだけ「①：このカードが召喚・特殊召喚に成功した時、〜」という
  遊戯王の書き方になっていた。発動タイミングを【条件】のイベント条件に統合し、
  【場所】【条件】【コスト】【制限】【発動後】の書式に揃えた。
  - 旧：`①：このカードが破壊された時、相手に800ポイントのダメージを与える。`
  - 新：`①：【条件】このカードが破壊された場合 相手に800ポイントのダメージを与える。`
- モンスターで【発動後】を省略した場合は、そのまま場（または手札）に残る。

### 追加

**【条件】に「〜した場合」を書けるようにした**

- これまでの「〜が存在する」といった状態の条件に加えて、盤面の出来事を
  条件にできるようにした。
  - 選べる出来事：召喚・特殊召喚された／召喚された／特殊召喚された／
    破壊された／墓地へ送られた／除外された／効果を発動した／攻撃宣言した／
    ダメージを受けた／ライフを回復した／カードをドローした
  - 「このカード自身が」または「自分の／相手の／お互いの」を選べる。
    カード単位の出来事は、属性・種族・カテゴリなどでさらに絞り込める。
  - 例：`【条件】相手の闇属性モンスターが特殊召喚された場合`
- この条件を持つ効果は誘発効果になり、手動では発動できない。
  持たない効果は、これまでどおり自分のメインフェイズに手動で発動する。
- 旧データの発動タイミングは、同じ意味のイベント条件として読み替える。

**【発動タイプ】任意（発動できる）と強制（発動する）の区別**

- 任意なら発動するか確認し、強制なら自動で発動する。
- カードテキストの語尾も「〜できる」「〜する」で書き分ける。

**手札誘発モンスターの、コストと発動後の区別**

- 【コスト】に「このカードを墓地へ送る」「このカードを除外する」を追加。
  発動と同時に送られるので、効果が無効になっても手札には戻らない。
- 効果の解決後に送りたい場合は【発動後】に「墓地へ送る」を指定する。
  この場合、発動時点ではまだ手札にある。
- サンプルに両方の型を追加（「警告の妖精」＝コスト型、「嘆きの巫子」＝発動後型）。

### 修正

- 誘発効果にも伏せカードの制約が掛かるようにした。伏せたターンの罠は
  誘発効果でも発動せず、裏側表示のモンスターは効果を発動しない。

---

## 2026-08-15 (3) — 【制限】の不具合修正と、発動できない理由の表示

### 修正

**カテゴリ単位の【制限】が、制限を書いていないカードにも消費されていた**

- 症状：カテゴリ単位の制限を1枚のカードに付けると、同じカテゴリの
  *別の* カード（制限を書いていないカード）が発動しただけで枠が減り、
  本来のカードが発動できなくなっていた。発動の順番によって結果が変わる。
- 具体例：「アララギの儀式」に『「アララギ」カードを含めて1ターンに1度』を
  付けていたが、儀式が特殊召喚した「アララギの巫女」の召喚時効果が発動した
  だけで枠を使い切ってしまい、儀式自身が発動できなくなっていた。
- 原因：発動の記録（`ActivationRecord`）にカードのカテゴリだけを持たせ、
  数えるときに「そのカテゴリを持つ全ての発動」を数えていた。
- 修正：制限を「枠（`limitKeys`）」として持たせ、**その制限を宣言している
  カードの発動だけが枠を消費する**ようにした。
  同じ制限を宣言したカードどうしは、これまでどおり枠を共有する。
  - `game/GameModels.kt` … `ActivationRecord.limitKeys` を追加
  - `game/GameEngine.kt` … `limitKey()` / `applicableLimits()` を追加し、
    `limitsAllow()` と `recordActivation()` を枠ベースに変更

**コストを支払えなかったときにカードが墓地へ送られていた**

- 症状：コストの支払いが成立しなかった場合でも、カードが墓地に置かれていた。
- 修正：発動そのものを取り消し、手札から出したところだったカードは手札に戻す。
  制限の枠も消費しない。（`game/GameEngine.kt` の `activateClause`）

### 追加

- **発動できない理由の表示**。カードをタップしたときのダイアログに、
  「【制限】により、このターンはもう発動できない。」のように理由を出すようにした。
  同じ内容をデュエルログにも残すので、不具合報告のときに原因を特定しやすい。
  （`game/GameEngine.kt` の `whyCannotActivate`、`ui/DuelScreen.kt`）
- **残り発動回数の表示**。制限が付いているカードは
  「【制限】このターンの残り発動回数: 1」を表示する。
  （`game/GameEngine.kt` の `remainingActivations`）
- **回帰テスト**。AI同士の通し対戦中、制限の枠が上限を超えて使われていないかを
  毎ターン検査するようにした。（`DuelSimulationTest`）
  この検査で上記のカテゴリ制限の不具合を検出しました。

### 再現できなかった報告

「終わらない祝祭」を発動 → 「アララギの儀式」を発動 → 「終わらない祝祭」が
もう一度発動できてしまった（ライフは回復しなかった）、という報告について。

同じ手順をテストで再現したところ、2回目は正しく発動できない状態
（`activatableClauses` が空、発動ボタンも無効）になり、再現しませんでした。
ライフが回復しなかったことから、効果自体は実行されていないと考えられます。

心当たりのある原因は次のとおりです。今回の版では、どれが起きたのか
デュエルログと画面表示から分かるようにしています。

- ターンをまたいでいた場合、制限はリセットされるので2回目の発動は正しい挙動です。
  その場合にライフが回復しないのは、相手の「静寂の結界」で無効にされた可能性があります
  （ログに「発動は無効になった」と残ります）。
- デッキには同じカードが3枚入ります。別の1枚であれば「このカード」単位の制限には
  掛かりません（この場合はライフが回復するはずです）。

同じことが起きた場合は、画面上部の「ログ」に出る文言を教えていただけると特定できます。

---

## 2026-08-15 (2) — 魔法・罠が発動できない不具合の修正、【発動後】【制限】の追加

### 修正

**魔法・罠カードがまったく発動できなかった**

原因は2つありました。

1. **【場所】の判定が間違っていた**。手札のカードは場所を「手札」として扱う一方、
   記述を省略したときの既定は「フィールド」だったため、場所を書いていないカードは
   手札から発動できなかった。ほとんどのカードは場所を書かないので、
   事実上どの魔法も発動できない状態だった。
   - 修正：魔法の「フィールドで発動」は、手札から魔法・罠ゾーンに出して発動する
     場合も含むように変更。【場所】に『手札』と書いたときだけ手札に置いたまま発動する。
     罠は従来どおりセットが必要（『手札』指定の罠を除く）。
   - `game/GameEngine.kt` の `canActivateFrom` / `needsFieldPlacement`

2. **発動処理が走る前に打ち切られていた**。カードの行動ダイアログが自前の
   `rememberCoroutineScope()` で発動処理を起動していたが、このスコープは
   ダイアログが閉じた瞬間に解約されるため、処理が始まる前に消えていた。
   通常召喚にも同じ影響が出ていた。
   - 修正：デュエル画面側のスコープを渡すように変更。`ui/DuelScreen.kt`

### 追加

- **【発動後】**。発動して解決したあとカードをどうするか
  （墓地へ送る／フィールドに残す／除外する／手札に戻す／デッキに戻す）。
  省略時は「墓地へ送る」。永続魔法のようにフィールドに残すカードが作れる。
- **【制限】**。1ターンに発動できる回数を、「このカード」「同名カード」
  「カテゴリ」の単位で指定できる。旧「1ターンに1度」チェックの置き換え。
  保存済みデータの古い値はそのまま読み込む。
- 【場所】が正しく効くようになったため、墓地・除外ゾーンの中身を見られる画面を追加。
  【場所】墓地の効果はここから発動できる。
- サンプルに永続魔法「終わらない祝祭」を追加。

---

## 2026-08-15 (1) — モンスター効果の初期タイミング修正

- 新しく追加した効果の発動タイミングの初期値が、エンジンが発動を見ない値
  （魔法・罠用の「発動時」）になっていたため、エディタで作ったモンスター効果が
  一切発動しなかった。カードの種類に応じた初期値にし、種類を変更したときは
  タイミングを有効な値に付け替えるようにした。

---

## 2026-08-14 — 初版

- カード作成（モンスター／魔法／罠、レベル・属性・種族・カテゴリ・攻守、
  カード名、端末からのイラスト取り込み）。
- 効果の自由記述。主語・目的語・修飾語・述語を選択肢から組み立てる。
  ①②… で複数の効果、【場所】【条件】【コスト】は効果番号の前後で適用範囲が変わる。
- カテゴリの自由設定。効果テキストから参照できる。
- 属性・種族・カテゴリのマスターデータ編集。
- デッキ構築。
- デュエル（ライフ8000、直接攻撃の制限、リリース、魔法・罠の発動タイミング、
  伏せ罠による無効化、AI対戦とホットシート2人対戦）。
